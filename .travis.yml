# arch: s390x
dist: bionic
addons:
  postgresql: "11"
language: ruby
# group: dev
# language: erlang

# rvm:
# - 2.6.5
# - 2.5.3
# - 2.7.0

cache: bundler

script:
- uname -a
- ps auxfw
# - uname -a
# - ruby -v
# - gem env
# - gem list
# - sudo gem list
